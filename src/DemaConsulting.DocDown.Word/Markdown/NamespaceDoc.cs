namespace DocDown.Word.Markdown;

/// <summary>
/// The internal document model and markdown mapping for Word extraction: blocks, inlines, lists
/// and tables read from a <c>.docx</c>, and the writers that turn them into the
/// <c>content.md</c> flow. Purely presentational and testable from a hand-built model; no public
/// types, so hosts never reference it directly.
/// </summary>
internal static class NamespaceDoc
{
}
