namespace DocDown.PowerPoint.OpenXml;

/// <summary>
/// Open XML extraction for <c>.pptx</c>. Reads every slide's text, title and speaker notes in
/// presentation order without requiring Microsoft PowerPoint — notes in particular exist only in
/// the file and appear in no render. This is the guaranteed content path on every platform; it
/// rasterizes no slides.
/// </summary>
internal static class NamespaceDoc
{
}
