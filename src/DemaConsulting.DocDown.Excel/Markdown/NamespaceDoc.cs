namespace DocDown.Excel.Markdown;

/// <summary>
/// The internal markdown emission for Excel extraction: turns a read workbook model into one
/// content part per worksheet and records the gaps the model and options imply. No public types,
/// so hosts never reference it directly.
/// </summary>
internal static class NamespaceDoc
{
}
