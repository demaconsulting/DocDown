namespace DocDown.Excel.OpenXml;

/// <summary>
/// Open XML extraction for <c>.xlsx</c>. Reads every worksheet's cell values verbatim at full
/// length, keeps each formula alongside its computed value, and cites facts by sheet and cell
/// address, without requiring Microsoft Excel. Look here when you need the numbers and prose a
/// workbook carries; it renders no pages, by design.
/// </summary>
internal static class NamespaceDoc
{
}
