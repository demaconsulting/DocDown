namespace DocDown.Word.OpenXml;

/// <summary>
/// Open XML extraction for <c>.docx</c>. Reads the document body, styles, real table grids,
/// document control (comments, footnotes, tracked changes) and embedded image parts without
/// requiring Microsoft Word or any native binary. Look here when you want to know exactly what
/// the Word backend can and cannot recover; it renders no pages.
/// </summary>
internal static class NamespaceDoc
{
}
