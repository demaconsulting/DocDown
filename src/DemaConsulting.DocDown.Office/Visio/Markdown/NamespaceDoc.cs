namespace DocDown.Visio.Markdown;

/// <summary>
/// The internal markdown emission for Visio extraction: turns a read drawing model into per-page
/// content carrying shape labels and the connector topology as a readable directed graph, and
/// records the plain inventory and notes the model and options imply. No public types, so hosts never reference it
/// directly.
/// </summary>
internal static class NamespaceDoc
{
}
